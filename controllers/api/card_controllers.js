const sqlQuery = require("../../models/sql_query");

module.exports = {
    postController(req, res) { // url: /api/cards

    },
    getController(req, res) { // url: /api/cards

    },
    idGetController(req, res) { // url: /api/cards/:id

    },
    idPutController(req, res) { // url: /api/cards/:id

    },
    idDelController(req, res) { // url: /api/cards/:id
        
    }
};